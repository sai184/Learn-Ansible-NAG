- name: wedding
  hosts: localhost
  vars:
    marr:Arjuns wedding
  tasks:
    - name: weedings
      ansible.builtin.debug:
        msg:"{{marr}}"
      vars:
        marr:Borras wedding

    - name: wedding
      ansible.builtin.debug:
        msg:"{{marr}}"

